# Scripts

Deploy and interaction scripts will go here after the contract is implemented.

Planned:

- Deploy `SkillSwap` to Sepolia (Foundry or Hardhat — team decision later)

For now, deploy via Remix + MetaMask.
